# ListViewSelectionCpp

Windows App SDK C++/WinRT sample demonstrating how to work around the automatic reset of a ListView's
selection index when its bound data source fires a collection changed event.
